{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Project creation</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

    <a href="{{ path('project') }}">
        {{ 'list.back'|trans }}
    </a>
{% endblock %}
